// cd [filename]
// ls

#include <iostream>

using namespace std;

int main(){
    int x = 0;
    x = 10;
    x = 100;
    x= -90;

    //int y; //garbage value
            //can compile with garbage values, but not good practice (set as zero as standard)

    cout << x << endl;

    return 0;
}