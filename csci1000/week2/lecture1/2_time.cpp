#include<iostream>
using namespace std;

/**
 * algorithm -> solution to a problem written in english
 * 
 * inputs = secondsInput
 * outputs = in a format -> daysOutput, hoursOutput, minutesOutput, secondsOutput
 * 
 * 
 * declare variables, days, hours, minutes, seconds & initialize to 0
 * 
 * get user input (in number of seconds)
 * 
 * daysOutput = secondsInput / 86400
 * secondsInput = secondsInput % 86400
 * 
 * hoursOutput = secondsInput / 3600
 * secondsInput = secondsInput % 3600
 * 
 * minutesOutput = secondsInput / 60
 * secondOutput = secondsInput % 60
 * 
*/

int main(){

    

    return 0;
}